[package]
name = "optimizer-helper"
version = "0.0.0"
edition = "2024"
license = "MIT"
publish = false

[dependencies]
optimizer-core = { path = "../optimizer-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
tracing = "0.1"
